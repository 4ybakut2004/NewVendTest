<% provide(:title, 'Request Types') %>

<script type="text/javascript">
	$(document).ready(function() {
		$('li#requestTypes').addClass('active');
	});
</script>

<div class="modal fade"
  id="newRequestType"
  tabindex="-1"
  role="dialog"
  aria-labelledby="newRequestTypeLabel"
  aria-hidden="true"
  data-backdrop="static">

  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button id="close-button"
          type="button"
          class="close"
          data-dismiss="modal"
          aria-hidden="true">
          &times;
        </button>
        <h4 class="modal-title" id="newRequestTypeLabel">Новый тип заявки</h4>
      </div> <!-- end of modal-header -->
      <div class="modal-body">
        <%= render 'form_new' %>
      </div>
    </div> <!-- end of modal-content -->
  </div> <!-- end of modal-dialog -->
</div> <!-- end of newRequestType -->

<div class="content">
  <%= render 'all' %>
  <%= render 'form_edit' %>
</div>